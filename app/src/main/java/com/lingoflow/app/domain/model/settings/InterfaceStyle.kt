package com.lingoflow.app.domain.model.settings

/** Visual style of the app, orthogonal to the light/dark theme mode. */
enum class InterfaceStyle {
    /** The default modern look. */
    MODERN,

    /** Warm paper / editorial look with serif headings. */
    EDITORIAL
}
